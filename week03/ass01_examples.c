#include <stdio.h>

// global variables
int x; 
int  y;     // assume 4 bytes

int main(void) {
	scanf("%d", &y);

	x = (y + 2000) * (y + 3000);
	printf("%d\n", x);

	a  = ((x*x + y*y) - x*y) * z

	if (b && c/4 < 10) || a == 9) {
		// do something here
	}

	return 0;
}
